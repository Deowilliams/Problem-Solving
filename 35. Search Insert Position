class Solution {
public:
    int searchInsert(vector<int>& nums, int target) {
        
            int l=nums.size();
            int i=0;
            while(i<l&&nums[i]<target)
                i++;
        return i;
        // int r=nums.size()-1;
        // int l=0;
        // int m=(r+l)/2;        
        // while(l<=r)
        // {
        //      m=(r+l)/2;
        //     if(nums[m]>target)
        //     r=m-1;
        //     else if(nums[m]<target)
        //     l=m+1;
        //     else 
        //         return m;
        // }
        // return l;
    }
};
